server:
  port: 10080
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          search-paths: config_repo
          uri: https://github.com/ilywsm/demo_zujian.git